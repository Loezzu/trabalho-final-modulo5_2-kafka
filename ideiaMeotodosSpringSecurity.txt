PERSONINFO API
- CREATE
- DELETE BY ID
- GET ALL / BY ID
- UPDATE BY ID
- UPDATE MY USER(CRIAR) // FEITO

ADDRESS API
- CREATE
- DELETE BY ID
- GET ALL / BY ID
- UPDATE BY ID
- UPDATE MY USER(CRIAR) // FEITO

USER API 
- CREATE
- DELETE BY ID
- GET COMPLETE / BY ID
- UPDATE BY ID
- LIKES DADOS
- LIKES RECEBIDOS (PRO)
- GET MACTHES
- DELETE MY USER(CRIAR)// FEITO
- UPDATE MY USER(CRIAR)// FEITO
- GET MY USER(CRIAR)// FEITO
- MOSTRAR USUARIOS COMPATIVEIS (PRO) (CRIAR) // FEITO

LIKE API *ADICINAR OS USERNAMES NO RETORNO DO MÉTODO // FEITO
- GET LIKES
- DELETA UM LIKE
- MOSTRA OS LIKES DE UM USER
- DELETA OS LIKES DE UM USUARIO
- DAR LIKE(MUDAR PARAMETROS PARA USAR USUARIO LOGADO) // FEITO
- DELETAR MEUS LIKES(CRIAR) // FEITO

MATCH API *ADICINAR OS USERNAMES NO RETORNO DO MÉTODO // FEITO
